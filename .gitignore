node_modules/
dist/
build/
out/
.next/
coverage/
*.log
.env
.env.*
!.env.example
.DS_Store
*.tsbuildinfo
.turbo/
cdk.out/
# Never commit client content
fixtures/real-content/

# Local runtime storage: uploaded presentations, built archives, and the file mail
# transport's spool. All of it is produced by running the app, none of it is source,
# and the spool holds live password-reset links. Regenerated by `npm run db:reset`.
.data/
