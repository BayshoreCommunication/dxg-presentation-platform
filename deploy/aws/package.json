{
  "name": "pmp-deploy",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit",
    "synth": "cdk synth --strict",
    "diff": "cdk diff",
    "deploy": "cdk deploy --exclusively Pmp-dev-Bootstrap",
    "test": "tsc --noEmit && cdk synth --strict --quiet"
  },
  "devDependencies": {
    "aws-cdk": "^2.170.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.170.0",
    "constructs": "^10.4.0"
  }
}
