{
  "name": "@pmp/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch src/index.ts",
    "start": "node src/index.ts",
    "type-check": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@pmp/auth": "*",
    "@pmp/db": "*",
    "@pmp/domain": "*",
    "@pmp/email": "*",
    "@pmp/files": "*",
    "express": "^5.1.0",
    "@pmp/format": "*"
  },
  "devDependencies": {
    "@types/express": "^5.0.0"
  }
}
